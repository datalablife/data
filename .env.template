# 🔐 环境变量配置模板
# 此文件不包含敏感信息，可以安全地提交到版本控制

# ==================== DATABASE 配置 ====================
DATABASE_HOST=<请设置实际值> # 从 .env 导入
DATABASE_PORT=<请设置实际值> # 从 .env 导入
POSTGRES_USER=<请设置实际值> # 从 .env 导入
POSTGRES_PASSWORD=<请设置实际值> # 从 .env 导入
POSTGRES_DB=<请设置实际值> # 从 .env 导入
DATABASE_URL=<请设置实际值> # 从 .env 导入

# ==================== SECURITY 配置 ====================
JWT_SECRET_KEY=<请设置实际值> # 从 .env 导入
JWT_ALGORITHM=<请设置实际值> # 从 .env 导入
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=<请设置实际值> # 从 .env 导入
JWT_REFRESH_TOKEN_EXPIRE_DAYS=<请设置实际值> # 从 .env 导入
RECAPTCHA_SECRET_KEY=<请设置实际值> # 从 .env 导入
OPENAI_MAX_TOKENS=<请设置实际值> # 从 .env 导入

# ==================== GENERAL 配置 ====================
APP_NAME=<请设置实际值> # 从 .env 导入
APP_VERSION=<请设置实际值> # 从 .env 导入
ENVIRONMENT=<请设置实际值> # 从 .env 导入
DEBUG=<请设置实际值> # 从 .env 导入
API_V1_PREFIX=<请设置实际值> # 从 .env 导入
ALLOWED_ORIGINS=<请设置实际值> # 从 .env 导入
ALLOWED_HOSTS=<请设置实际值> # 从 .env 导入
LOG_LEVEL=<请设置实际值> # 从 .env 导入
LOG_FILE=<请设置实际值> # 从 .env 导入
RATE_LIMIT_LOGIN=<请设置实际值> # 从 .env 导入
RATE_LIMIT_REGISTER=<请设置实际值> # 从 .env 导入
RATE_LIMIT_REFRESH=<请设置实际值> # 从 .env 导入
PASSWORD_MIN_LENGTH=<请设置实际值> # 从 .env 导入
PASSWORD_REQUIRE_UPPERCASE=<请设置实际值> # 从 .env 导入
PASSWORD_REQUIRE_LOWERCASE=<请设置实际值> # 从 .env 导入
PASSWORD_REQUIRE_NUMBERS=<请设置实际值> # 从 .env 导入
PASSWORD_REQUIRE_SPECIAL_CHARS=<请设置实际值> # 从 .env 导入
MAX_LOGIN_ATTEMPTS=<请设置实际值> # 从 .env 导入
ACCOUNT_LOCKOUT_DURATION_MINUTES=<请设置实际值> # 从 .env 导入
SESSION_TIMEOUT_MINUTES=<请设置实际值> # 从 .env 导入
MAX_FILE_SIZE_MB=<请设置实际值> # 从 .env 导入
UPLOAD_DIR=<请设置实际值> # 从 .env 导入
ALLOWED_FILE_TYPES=<请设置实际值> # 从 .env 导入
NEXT_PUBLIC_API_URL=<请设置实际值> # 从 .env 导入
NEXT_PUBLIC_APP_NAME=<请设置实际值> # 从 .env 导入

# ==================== API 配置 ====================
RECAPTCHA_SITE_KEY=<请设置实际值> # 从 .env 导入
RECAPTCHA_VERIFY_URL=<请设置实际值> # 从 .env 导入
OPENAI_API_KEY=<请设置实际值> # 从 .env 导入
OPENAI_MODEL=<请设置实际值> # 从 .env 导入
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=<请设置实际值> # 从 .env 导入

# ==================== CACHE 配置 ====================
REDIS_HOST=<请设置实际值> # 从 .env 导入
REDIS_PORT=<请设置实际值> # 从 .env 导入
REDIS_PASSWORD=<请设置实际值> # 从 .env 导入
REDIS_DB=<请设置实际值> # 从 .env 导入

# ==================== EMAIL 配置 ====================
SMTP_HOST=<请设置实际值> # 从 .env 导入
SMTP_PORT=<请设置实际值> # 从 .env 导入
SMTP_USER=<请设置实际值> # 从 .env 导入
SMTP_PASSWORD=<请设置实际值> # 从 .env 导入
SMTP_TLS=<请设置实际值> # 从 .env 导入

